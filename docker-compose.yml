version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vite_app
    ports:
      - "3000:3000"   # mapeia localhost:3000 -> container:3000
    environment:
      - NODE_ENV=production
    restart: unless-stopped